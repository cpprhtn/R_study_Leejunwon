dotchart
x <- 1:10
y <- x-1
plot(x,y)
datchart(x,y)
datchart(x,y)
x <- 1:10
y <- x-1
plot(x,y)
datchart(x,y)
dotchart(x,y)
plot(x,y)
dotchart(x,y)
plot(mtcars$mpg)
dotchart(mtcars$mpg, labels=row.names(mtcars), cex=0.7) #x, y축이 바뀐모습
dotchart(mtcars$mpg, labels=row.names(mtcars), cex=1) #x, y축이 바뀐모습
dotchart(mtcars$mpg, labels=row.names(mtcars), cex=0.5) #x, y축이 바뀐모습
dotchart(mtcars$mpg, labels=row.names(mtcars), cex=1.2) #x, y축이 바뀐모습
carmpg <- mtcars[order(mtcars$mpg),] #mpg가 작은순서대로 나열
carmpg$cyl <- factor(carmpg$cyl)
class(carmpg$cyl)
carmpg$color[carmpg$cyl==4] <- "blue"
carmpg$color[carmpg$cyl==6] <- "green"
carmpg$color[carmpg$cyl==8]  <- "red"
dotchart(carmpg$mpg, labels=row.names(carmpg),
groups=carmpg$cyl, cex=0.8,
xlab="Miles Per Gallon", color=carmpg$color, main="Milage depending on numbers of cylinder")
plot(mtcars$wt,mtcars$mpg)
dotchart(mtcars$wt,mtcars$mpg)
mtcars
par(mfrow=c(1,2))
plot(mtcars$wt,mtcars$mpg)
dotchart(mtcars$wt,mtcars$mpg)
#dotchart를 이용해 데이터를 변형시켜보자.
dotchart(mtcars$wt,mtcars$mpg, labels=row.names(mtcars), cex=1.2)
#dotchart를 이용해 데이터를 변형시켜보자.
dotchart(mtcars$wt,mtcars$mpg, labels=row.names(mtcars), cex=1)
#dotchart를 이용해 데이터를 변형시켜보자.
par(mfrow=c(1,1))
dotchart(mtcars$wt,mtcars$mpg, labels=row.names(mtcars), cex=1)
dotchart(mtcars$mpg,mtcars$wt, labels=row.names(mtcars), cex=1)
dotchart(mtcars$mpg,mtcars$wt, labels=row.names(mtcars), cex=0.8)
dotchart(mtcars$mpg,mtcars$gear, labels=row.names(mtcars), cex=0.8)
dotchart(mtcars$gear,mtcars$mpg, labels=row.names(mtcars), cex=0.8)
dotchart(mtcars$mpg,mtcars$gear, labels=row.names(mtcars), cex=0.8)
dotchart(mtcars$mpg,mtcars$gear, labels=row.names(mtcars), cex=0.8,xlab = "mpg",ylab = "gear")
dotchart(mtcars$mpg)
dotchart(mtcars$mpg,labels = row.names(mtcars))
dotchart(mtcars$mpg,labels = row.names(mtcars),main = mpㅎ)
dotchart(mtcars$mpg,labels = row.names(mtcars),main = "mpㅎ")
dotchart(mtcars$mpg,labels = row.names(mtcars))
#먼저 mpg값을 정렬해준다.
sort(mtcars$mpg)
#먼저 mpg값을 정렬해준다.
newmpg <- sort(mtcars$mpg)
newmpg
#먼저 mpg값을 정렬해준다.
newmpg <- sort(mtcars)
newmpg
?order
#먼저 mpg값을 정렬해준다.
newmpg <- mtcars[order(mtcars$mpg),] #mpg가 작은순서대로 정렬(sort)
newmpg
#먼저 mpg값을 정렬해준다.
sort_mpg <- mtcars[order(mtcars$mpg),] #mpg가 작은순서대로 정렬(sort)
sort_mpg
dotchart(carmpg$mpg, labels=row.names(carmpg),
groups=carmpg$gear, cex=0.8,
xlab="Miles Per Gallon", color=carmpg$color, main="Milage depending on numbers of cylinder")
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8,
xlab="Miles Per Gallon", color=sort_mpg$color, main="Milage depending on numbers of cylinder")
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8,
xlab="Miles Per Gallon", main="Milage depending on numbers of cylinder")
class(mtcars$gear)
sort_mpg$gear <- factor(mtcars$gear)
class(sort_mpg$gear)
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8,
xlab="Miles Per Gallon", main="Milage depending on numbers of cylinder")
#먼저 mpg값을 정렬해준다.
par(mfrow=c(1,2))
sort_mpg <- mtcars[order(mtcars$mpg),] #mpg가 작은순서대로 정렬(sort)
class(mtcars$gear)
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8,
xlab="Miles Per Gallon(mpg)", main="gear's class type is numeric")
mtcars
par(mfrow=c(1,2))
plot(mtcars$wt,mtcars$mpg)
dotchart(mtcars$wt,mtcars$mpg)
dotchart가 더 알아보기 편하다.
#dotchart를 이용해 데이터를 변형시켜보자.
#mpg값을 gear에따라 분류해보자.
par(mfrow=c(1,1))
dotchart(mtcars$mpg,labels = row.names(mtcars))
#먼저 mpg값을 정렬해준다.
par(mfrow=c(1,2))
sort_mpg <- mtcars[order(mtcars$mpg),] #mpg가 작은순서대로 정렬(sort)
class(mtcars$gear)
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8,
xlab="Miles Per Gallon(mpg)", main="gear's class type is numeric")
sort_mpg$gear <- factor(mtcars$gear)
class(sort_mpg$gear)
sort_mpg
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8,
xlab="Miles Per Gallon", main="Milage depending on numbers of cylinder")
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8,
xlab="Miles Per Gallon(mpg)", main="gear's class type is numeric")
#dotchart를 이용해 데이터를 변형시켜보자.
#mpg값을 gear에따라 분류해보자.
par(mfrow=c(1,1))
dotchart(mtcars$mpg,labels = row.names(mtcars))
#먼저 mpg값을 정렬해준다.
par(mfrow=c(1,2))
sort_mpg <- mtcars[order(mtcars$mpg),] #mpg가 작은순서대로 정렬(sort)
class(mtcars$gear)
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8,
xlab="Miles Per Gallon(mpg)", main="gear's class type is numeric")
sort_mpg$gear <- factor(mtcars$gear)
class(sort_mpg$gear)
sort_mpg
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8,
xlab="Miles Per Gallon(mpg)", main="gear's class type is factor")
#먼저 mpg값을 정렬해준다.
par(mfrow=c(1,2))
sort_mpg <- mtcars[order(mtcars$mpg),] #mpg가 작은순서대로 정렬(sort)
class(mtcars$gear)
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg), cex=0.8,
xlab="Miles Per Gallon(mpg)", main="gear's class type is numeric")
sort_mpg$gear <- factor(mtcars$gear)
class(sort_mpg$gear)
sort_mpg
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8,
xlab="Miles Per Gallon(mpg)", main="gear's class type is factor")
lines(sort_mpg$mpg,sort_mpg$gear,col = "purple")
sort_mpg$color[sort_mpg$gear==3] <-"blue"
sort_mpg$color[sort_mpg$gear==4] <-"red"
sort_mpg$color[sort_mpg$gear==5] <-"dark green"
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8, color = sort_mpg$color
xlab="Miles Per Gallon(mpg)", main="gear's class type is factor")
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=0.8, color = sort_mpg$color,
xlab="Miles Per Gallon(mpg)", main="gear's class type is factor")
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=1, color = sort_mpg$color,
xlab="Miles Per Gallon(mpg)", main="gear's class type is factor")
par(mfrow=c(1,1))
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=1, color = sort_mpg$color,
xlab="Miles Per Gallon(mpg)", main="gear's class type is factor")
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=1, color = sort_mpg$color, ylab = "gear"
xlab="Miles Per Gallon(mpg)", main="gear's class type is factor")
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=1, color = sort_mpg$color, ylab = "gear",
xlab="Miles Per Gallon(mpg)", main="gear's class type is factor")
?ylab
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, groups = "gear", cex=1, color = sort_mpg$color, ylab = "gear",
xlab="Miles Per Gallon(mpg)", main="gear's class type is factor")
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=1, color = sort_mpg$color,
xlab="Miles Per Gallon(mpg)", main="gear's class type is factor")
plot(sort_mpg$mpg,labels = row.names(mtcars))
plot(sort_mpg$mpg,labels = row.names(sort_mpg))
plot(sort_mpg$mpg)
plot(sort_mpg$mpg,sort_mpg$gear)
plot(sort_mpg$gear,sort_mpg$mpg)
dotchart(sort_mpg$mpg, labels=row.names(sort_mpg),
groups=sort_mpg$gear, cex=1, color = sort_mpg$color,
xlab="Miles Per Gallon(mpg)", main="gear's class type is factor")
library(Math)
library(math)
library(MATH)
plot(mtcars$wt,mtcars$mpg)
dotchart(mtcars$wt,mtcars$mpg)
plot(mtcars$wt,mtcars$mpg)
dotchart(mtcars$wt,mtcars$mpg)
plot(mtcars$wt,mtcars$mpg)
dotchart(mtcars$wt,mtcars$mpg)
plot(mtcars$wt,mtcars$mpg)
plot(mtcars$wt,mtcars$mpg,type = "l")
plot(mtcars$wt,mtcars$mpg,type = "b")
plot(mtcars$wt,mtcars$mpg,type = "h")
par(mfrow=c(2,2))
par(mfrow=c(2,2)) #화면분할
arr <= c("l","p","h","b")
for(i in 1:4){
plot(mtcars$wt,mtcars$mpg,type = arr[i])
}
arr <= c("l","p","h","b")
for(i in 1:4){
plot(mtcars$wt,mtcars$mpg,type = arr[i])
}
arr <- c("l","p","h","b")
for(i in 1:4){
plot(mtcars$wt,mtcars$mpg,type = arr[i])
}
par(mfrow=c(2,2)) #화면분할
arr <- c("l","p","h","b")
for(i in 1:4){
plot(mtcars$wt,mtcars$mpg,type = arr[i])
}
par(mfrow=c(1,1))
dotchart(mtcars$wt,mtcars$mpg)
par(mfrow=c(2,2)) #화면분할
arr <- c("l","p","h","b")
for(i in 1:4){
plot(mtcars$mpg,mtcars$gear,type = arr[i])
}
mtcars
plot(mtcars$mpg,mtcars$gear)
plot(mtcars$mpg,mtcars$gear,type = "l")
#mtcars에 있는 mpg와 gear를 시각화해보려한다.
#먼저 지난번에 배운 plot로 나타내보자.
arr <- c("l","p","b","h")
for(i in 1:4){
plot(mtcars$mpg,mtcars$gear,type = arr[i])
}
par(mfrow = c(2,2)) #화면분할
for(i in 1:4){
plot(mtcars$mpg,mtcars$gear,type = arr[i])
}
#plot그래프로는 데이터를 알아보기가 힘들다.
#dotchart를 이용해보자.
par(mfrow=c(1,1))
dotchart(mtcars$mpg,mtcars$gear)
dotchart(mtcars$mpg,labels = row.names(mtcars))
#먼저 mpg값을 정렬해주자.
sort_mpg <- mtcars[order(mtcars$mpg),] #mpg값이 작은 순서대로 정렬
?order
sort_mpg
class(mtcars$gear)
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg),xlab = "Miles Per Gallon(mpg)",
main = "Gear's class type is numeric")
#정렬 전보다 보기가 편해졌다.
sort_mpg$gear <- factor(mtcars$gear)
class(sort_mpg$gear)
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg),xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear
main = "Gear's class type is factor")
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg),xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear,
main = "Gear's class type is factor")
sort_mpg
#먼저 mpg값을 정렬해주자.
sort_mpg <- mtcars[order(mtcars$mpg),] #mpg값이 작은 순서대로 정렬(sort)
sort_mpg
class(mtcars$gear)
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg),xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear,
main = "Gear's class type is numeric")
mtcars
#mtcars에 있는 data중 mpg와 gear를 시각화해보려 한다.
#먼저 지난시간에 배운 plot로 나타내보자.
plot(mtcars$mpg,mtcars$gear)
#데이터 형태가 이상하다.
#다른 타입도 확인해보자.
arr <- c("p","l","h","b")
arr <- c("p","l","h","b")
par(mfrow=c(2,2)) #화면을 2,2로 분할한다.
for(i in 1:4){
plot(mtcars$mpg,mtcars$gear, type = arr[i])
}
#다른 타입의 그래프들 모두 형태가 이상하다.
#dotchart로 나타내보자.
dotchart(mtcars$mpg,mtcars$gear)
#다른 타입의 그래프들 모두 형태가 이상하다.
#dotchart로 나타내보자.
par(mfrow=c(1,1))
dotchart(mtcars$mpg,mtcars$gear)
#rowname를 먼저 나타내주자.
dotchart(mtcars$mpg,labels = row.names(mtcars))
?order
#mpg값을 정렬해주자.
sort_mpg <- mtcars[order(mtcars$mpg),] #mpg의 값을 작은 순서대로 sort(정렬)해준다.
sort_mpg
class(mtcars$gear)
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear,
main = "Gear's class type is numeric")
#gear에 따라 자료가 묶여서 처리되었다.
#그러나 그래프만 보고는 gear의 값을 알수가 없다.
#이를 해결해보자.
sort_mpg$gear <- factor(sort_mpg$gear)
class(sort_mpg$g)
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear,
main = "Gear's class type is factor")
par(mfrow=c(1,2))
#mpg값을 정렬해주자.
sort_mpg <- mtcars[order(mtcars$mpg),] #mpg의 값을 작은 순서대로 sort(정렬)해준다.
sort_mpg
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear,
main = "Gear's class type is numeric")
#gear에 따라 자료가 묶여서 처리되었다.
#그러나 그래프만 보고는 gear의 값을 알수가 없다.
#이를 해결해보자.
sort_mpg$gear <- factor(sort_mpg$gear)
class(sort_mpg$g)
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear,
main = "Gear's class type is factor")
#sort_mpg의 gear 값이 3일때 sort_mpg의 color항목에 "blue"를 넣는다.
#4일때는 red
#5일때는 dark green
par(mfrow=c(1,1))
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear, color = sort_mpg$color
main = "Gear's class type is factor")
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear, color = sort_mpg$color,
main = "Gear's class type is factor")
sort_mpg$color[sort_mpg$gear==3] <- "blue"
sort_mpg$color[sort_mpg$gear==4] <- "red"
sort_mpg$color[sort_mpg$gear==5] <- "dark green"
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear, color = sort_mpg$color,
main = "Gear's class type is factor")
#sort_mpg의 gear 값이 3일때 sort_mpg의 color항목에 "blue"를 넣는다.
#4일때는 red
#5일때는 dark green
par(mfrow=c(1,2))
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear,
main = "Gear's class type is factor")
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear, color = sort_mpg$color,
main = "Gear's class type is factor")
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear,
main = "No color")
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear, color = sort_mpg$color,
main = "Have color")
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear, color = sort_mpg$color, cex=1.2
main = "Have color")
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear, color = sort_mpg$color, cex=1.2,
main = "Have color")
dotchart(sort_mpg$mpg,labels = row.names(sort_mpg), xlab = "Miles Per Gallon(mpg)",
groups = sort_mpg$gear, color = sort_mpg$color, cex=0.8, #글자크기
main = "Have color")
